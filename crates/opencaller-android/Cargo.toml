[package]
name = "opencaller-android"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "JNI bridge: opencaller-core for the Android shell"

[lib]
crate-type = ["cdylib"]

[dependencies]
opencaller-core = { path = "../opencaller-core" }
jni = "0.21"
